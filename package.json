{
  "name": "serverless-example",
  "description": "This is a example for serverless framework",
  "license": "MIT",
  "scripts": {
    "lint": "yarn eslint ./api/**.js ./api/model/**.js",
    "test": "yarn lint",
    "update": "yarn && cd api && yarn"
  },
  "dependencies": {
    "aws-sdk": "^2.389.0",
    "http-status": "^1.3.1",
    "serverless": "^1.40.0",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "eslint": "^5.15.3"
  }
}
